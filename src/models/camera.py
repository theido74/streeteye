class Camera:
    def __init__(self, idCamera, nom, url, location, distance):
        self.idCamera = idCamera
        self.nom = nom
        self.url = url
        self.location = location
        self.distance = distance

